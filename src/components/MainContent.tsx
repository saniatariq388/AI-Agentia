

export default function MainContent() {
  

  return (
    <div className="min-h-screen bg-[#0B0014] text-white">
      {/* Navigation */}
      <header className="border-b border-white/10">{/* ... (rest of the header code) ... */}</header>

      {/* Mobile Menu */}
      {/* ... (mobile menu code) ... */}

      {/* Hero Section */}
      <section className="container mx-auto px-4 py-16">{/* ... (hero section code) ... */}</section>

      {/* Services Section */}
      <section id="services" className="container mx-auto px-4 py-16">
        {/* ... (services section code) ... */}
      </section>

      {/* Features Section */}
      <section id="features" className="container mx-auto px-4 py-16">
        {/* ... (features section code) ... */}
      </section>

      {/* Process Steps */}
      <section className="container mx-auto px-4 py-16">{/* ... (process steps section code) ... */}</section>

      {/* Testimonials */}
      <section className="container mx-auto px-4 py-16">{/* ... (testimonials section code) ... */}</section>

      {/* CTA Section */}
      <section className="bg-gradient-to-r from-pink-500 to-purple-600 py-16">
        {/* ... (CTA section code) ... */}
      </section>

      {/* Footer */}
      <footer className="border-t border-white/10 bg-black/20">{/* ... (footer code) ... */}</footer>
    </div>
  )
}

